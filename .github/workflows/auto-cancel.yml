name: Auto Cancel Workflow
run-name: Auto Cancel
on: pull_request
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  auto-cancel:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: sleep 120
