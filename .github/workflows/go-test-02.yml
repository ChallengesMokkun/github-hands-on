name: Go unit test
run-name: Go Unit Test
on:
  pull_request:
    paths:
      - go/
jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: '1.26.1'
      - run: go test -v ./...
